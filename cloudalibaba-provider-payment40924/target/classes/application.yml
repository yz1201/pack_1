server:
  port: 40924

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #配置Nacos地址
#服务暴露
management:
  endpoints:
    web:
      exposure:
        include: '*'